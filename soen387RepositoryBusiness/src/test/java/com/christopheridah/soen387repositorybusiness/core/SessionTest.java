/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.christopheridah.soen387repositorybusiness.core;

import java.io.FileOutputStream;
import java.io.IOException;
import java.io.OutputStream;
import java.security.NoSuchAlgorithmException;
import java.util.Properties;
import org.junit.Test;

/**
 *
 * @author chris
 */
public class SessionTest {
    
    
 /*   @Test 
    
    public void createNewUser () throws IOException, NoSuchAlgorithmException {
        
        try (OutputStream output = new FileOutputStream ("/config.properties")) {
            
            Properties prop = new Properties();
            
            prop.setProperty("json", "/users.json");
            
            prop.store(output, "filepath to user list file");
            
        }
        
        User newUser = new User ("chris", "cake", false);
        
        newUser.createProfile();
        
        Session testSession = new Session (newUser);
        
        assert(testSession.loadProfile(newUser.getUserId(), newUser.getPassword()));
        
    } */
    
}
